import 'package:flutter/material.dart';

class estadisticos extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Estadísticos'),
      ),
      body: Center(
        child: Text('Esta es la pantalla de los estadísticos'),
      ),
    );
  }
}
