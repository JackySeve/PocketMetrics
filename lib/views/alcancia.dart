import 'package:flutter/material.dart';

class alcancia extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Alcancía'),
      ),
      body: Center(
        child: Text('Esta es la pantalla de la alcancía'),
      ),
    );
  }
}
